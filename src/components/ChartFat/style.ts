import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        width: '100%',
        alignItems: "center",
        justifyContent: "space-around",
    },
    chart: {
        width: 120
    }
});