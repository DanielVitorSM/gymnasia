import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        paddingHorizontal: 20
    },
    row: {
        flexDirection: 'row',
        marginBottom: 5
    },
    section: {
        marginTop: 20,
        marginBottom: 10
    }
});